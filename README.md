# Ekai_Gateway

Small personal scripts related to Reddit and ChatGPT analysis.

Usage
- Install dependencies: `pip install -r requirements.txt`
- Run analysis scripts directly with `python`.

Notes
- The file `reddit_posts.json` is large/sensitive and is excluded via `.gitignore`.
